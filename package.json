{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "getReady": "rimraf .nuxt .output docs && npm run generate && shx mkdir docs && shx cp -r .output/public/* docs/"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.0.0",
    "@mdi/font": "^7.4.47",
    "axios": "^1.11.0",
    "nuxt": "^4.0.1",
    "sass": "^1.90.0",
    "vue": "3.5.13",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "shx": "^0.4.0",
    "vite-plugin-vuetify": "^2.1.2",
    "vuetify": "^3.9.3"
  }
}
